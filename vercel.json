{
  "functions": {
    "api/**/*.js": {
      "includeFiles": "lib/**"
    }
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "handle": "filesystem" }
  ],
  "env": {
    "NODE_OPTIONS": "--openssl-legacy-provider"
  }
}